diff --git a/agent/title_generator.py b/agent/title_generator.py
index 5323f8bd0..75db115e7 100644
--- a/agent/title_generator.py
+++ b/agent/title_generator.py
@@ -709,6 +709,7 @@ def maybe_auto_title(
     main_runtime: dict = None,
     title_callback: Optional[TitleCallback] = None,
     runtime_validator: Optional[RuntimeValidator] = None,
+    upgrade_with_model: bool = True,
 ) -> None:
     """Title a session from its opening message: instant, then upgraded.
 
@@ -745,7 +746,32 @@ def maybe_auto_title(
         logger.debug("Auto-title skipped: auxiliary.title_generation.enabled=false")
         return
 
-    apply_instant_title(session_db, session_id, user_message, title_callback)
+    instant_title = apply_instant_title(
+        session_db, session_id, user_message, title_callback
+    )
+
+    if not upgrade_with_model:
+        # Desktop titles are cosmetic. Keep the user's opening intent as the
+        # durable title instead of spending a second provider request behind a
+        # one-round chat. If the inline fast path declined a duplicate title,
+        # resolve that collision locally so repeated greetings still get a
+        # visible sidebar entry without asking a model.
+        if instant_title is None and _session_is_untitled(session_db, session_id):
+            title = derive_title(user_message)
+            if title:
+                try:
+                    persisted = _persist_session_title(
+                        session_db,
+                        session_id,
+                        title,
+                        source="derived",
+                        dedupe=True,
+                    )
+                    if persisted and title_callback is not None:
+                        title_callback(persisted, "derived")
+                except Exception:
+                    logger.debug("Local-only title fallback failed", exc_info=True)
+        return
 
     thread = threading.Thread(
         target=auto_title_session,
diff --git a/agent/turn_context.py b/agent/turn_context.py
index 5982e3846..e369af54a 100644
--- a/agent/turn_context.py
+++ b/agent/turn_context.py
@@ -253,6 +253,12 @@ def _maybe_title_session_at_turn_start(agent: Any, messages: List[Any]) -> None:
                 getattr(agent, "model", None) == _model
                 and getattr(agent, "provider", None) == _provider
             ),
+            # Desktop already has a useful local title derived from the
+            # opening prompt. Do not spend a second provider request merely
+            # to polish sidebar text; other surfaces keep upstream behavior.
+            upgrade_with_model=(
+                str(getattr(agent, "platform", "") or "").lower() != "desktop"
+            ),
         )
     except Exception:
         logger.debug("Turn-start auto-title dispatch failed", exc_info=True)
diff --git a/tests/agent/test_title_generator.py b/tests/agent/test_title_generator.py
index e4ae76b8a..56b1b007e 100644
--- a/tests/agent/test_title_generator.py
+++ b/tests/agent/test_title_generator.py
@@ -301,6 +301,42 @@ class TestMaybeAutoTitle:
         assert db.get_session_title("sess-1") == "fix the flaky auth test in login"
         assert db.get_session_title_source("sess-1") == "derived"
 
+    def test_local_only_title_never_starts_model_worker(self, tmp_path):
+        db = SessionDB(tmp_path / "state.db")
+        db.create_session(session_id="sess-1", source="desktop")
+
+        with patch("agent.title_generator.auto_title_session") as mock_auto:
+            maybe_auto_title(
+                db,
+                "sess-1",
+                "Chào em",
+                [],
+                upgrade_with_model=False,
+            )
+
+        assert db.get_session_title("sess-1") == "Chào em"
+        assert db.get_session_title_source("sess-1") == "derived"
+        mock_auto.assert_not_called()
+
+    def test_local_only_title_dedupes_without_model_worker(self, tmp_path):
+        db = SessionDB(tmp_path / "state.db")
+        db.create_session(session_id="taken", source="desktop")
+        db.set_session_title("taken", "Chào em")
+        db.create_session(session_id="sess-1", source="desktop")
+
+        with patch("agent.title_generator.auto_title_session") as mock_auto:
+            maybe_auto_title(
+                db,
+                "sess-1",
+                "Chào em",
+                [],
+                upgrade_with_model=False,
+            )
+
+        assert db.get_session_title("sess-1") == "Chào em #2"
+        assert db.get_session_title_source("sess-1") == "derived"
+        mock_auto.assert_not_called()
+
     def test_skips_machine_authored_opening_messages(self, tmp_path):
         """A compaction handoff is not a user request and must not title."""
         db = SessionDB(tmp_path / "state.db")
diff --git a/tests/agent/test_turn_context.py b/tests/agent/test_turn_context.py
index 9c087111e..9f893b619 100644
--- a/tests/agent/test_turn_context.py
+++ b/tests/agent/test_turn_context.py
@@ -444,6 +444,19 @@ def test_prologue_titles_the_surfaces_a_person_reads(platform):
     assert _title_turn(platform).called
 
 
+def test_desktop_prologue_disables_model_title_upgrade():
+    titler = _title_turn("desktop", "Chào em")
+
+    assert titler.call_args.kwargs["upgrade_with_model"] is False
+
+
+@pytest.mark.parametrize("platform", ["cli", "telegram", "acp", None])
+def test_non_desktop_prologue_keeps_model_title_upgrade(platform):
+    titler = _title_turn(platform)
+
+    assert titler.call_args.kwargs["upgrade_with_model"] is True
+
+
 @pytest.mark.parametrize("platform", ["cron", "CRON", "subagent"])
 def test_prologue_does_not_title_machine_driven_runs(platform):
     """Cron names its own session after the job, and nobody opens a subagent's.
